# Factory Monitor

Implement the accepted MASTER_PLAN.md in this isolated repository. Never modify the synced ChatGPT project or FDE toolbox. No production camera images or secrets may be sent to coding agents or remote services. Local replay fixtures must be marked synthetic. Cloud analysis is disabled by default; runtime services bind loopback.

Use Python 3.12, a project-local .venv, PySide6, SQLite. Windows WGC (display fallback), macOS ScreenCaptureKit. Do not substitute a screenshot loop and label it WGC/ScreenCaptureKit. No camera/NVR connection prerequisite.

Read STATUS.md, TASKS.md, DECISIONS.md and TEST_REPORT.md before repeating work. Only claim verified behavior. Windows acceptance requires actual Windows evidence; no test fixture or Mac build may satisfy it. Two same-category failed fixes require root-cause diagnosis.

Model routing: Luna extraction, Terra deterministic implementation, Sol capture/concurrency/integration, root architecture and final Gate. One concentrated final review; do not spawn a reviewer for every small task. Workers must not edit each other's owned files or commit unstaged work belonging to another worker.
