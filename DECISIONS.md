# Decisions

- D001: Independent repository, branch implement/v1; no edits to FDE or ChatGPT source mirror.
- D002: The accepted in-chat plan is the approved spec. Execute continuously; no additional design approval gates.
- D003: User cost routing overrides per-task multi-review and five-fix-loop skill defaults: two same-category failures trigger diagnosis; one concentrated final independent review.
- D004: Field acceptance will remain FAIL until actual Windows/Mac footage, switching and 72h evidence exist. Synthetic replay may validate software behavior only.
- D005: Dependency environment uses bundled Python3.12; no global Python changes. No paid cloud inference, no real camera upload. Local model download permitted for authorized implementation.
- D006: Independent review demonstrated that generic global clicks cannot prove the actual target under overlays and coordinate scaling. Runtime OS-click dispatch is disabled unconditionally. Automatic enlargement/return remains undelivered until a target-client adapter is implemented and validated; manual client operation is the explicit fallback, with unavailable coverage shown.
- D007: Freshness is per camera. A shared monitoring-client clock cannot establish ten fresh feeds; missing/stale camera-specific heartbeat proof disables that camera's analysis and records evidence gaps.
- D008: A real local Qwen ten-event synthetic burst missed the frozen SLA (1 completed, 9 timeouts). Preserve all failures. Do not change the 15-second deadline, remove cameras, or suppress review to claim success. A bounded component concurrency diagnostic does not replace this runtime result or justify a hardware purchase. The minimum necessary hardware tier and current quote remain undetermined until the Windows host and actual capture/model workload are measured.
- D009: Delivery label is engineering preview v0.1, not accepted V1. Missing automatic switching, review capacity and target-OS/field evidence are mandatory work, not OPTIONAL/BACKLOG. Further unrelated polish, twenty cameras, mobile push and commercialization remain out of scope.
- D010: Supplied 2026-09-22 Windows synthetic evidence disproves target-device ten-camera capacity readiness despite earlier hosted CI. Prioritize measured evidence hot paths, orderly queue shutdown, accurate loss accounting and independent evidence-integrity reporting. Keep original load/windows/thresholds; do not treat a docs-only main update, larger queues/timeouts, added frameworks or hardware procurement as a fix. See docs/WINDOWS_RETEST_AUDIT_2026-09-22_zh.md; diagnosis plan only, runtime unchanged in this audit.
- D011: Preserve the in-progress synthetic Windows 30-minute run. The user reports two fixes in a separate copy for recording throughput and stuck shutdown, with the original installation untouched; collect and audit those patches, test tools and results before merging or repeating diagnostics. These fixes are reported, not yet verified here. Deliver fixed-revision source and local setup now; a Windows portable executable remains future work requiring target-system build and clean-account proof. Upgrade in a separate directory after a consistent stopped backup; never share a writable evidence database across versions. A quiet 30-minute session does not prove concurrent evidence/retention capacity. Laya and conditional TypeSafe Jev stay outside the monitoring runtime, eligible only for a later bounded development-text comparison against rules and existing model routing.
- D012 (2026-09-23, explicit user correction): Prioritize a bounded Windows functional delivery: ten distinct live Seetong views, actual person detection/tracking and frozen event rules, actual local visual review, visible screen alerts and an actual feature screen recording. Full Q1–Q7 and a durable per-frame audit are no longer prerequisites for this supervised pilot. Keep the original production acceptance requirements unchanged. Before the pilot, prove isolation, event persistence, a working stop path, visible failures and an effective capacity/retention guard; the known delete-order risk may be isolated by preventing that path during a clearly bounded pilot, never by silently losing events or disabling disk protection. Fix or isolate directly blocking defects first; complete durable audit, recovery, sustained retention and full stress validation before unattended long runs/release. Use the latest Windows repair copy, not the older public main. Lack of the new Windows source here prevents independent acceptance, not local execution by the Windows agent that already has it. No camera footage goes to coding agents or GitHub. A synthetic UI movie does not fulfill the requested real-input analysis and alert demonstration.

- D013 (2026-09-23, mainland deployment): The final Windows host uses mainland China Internet without VPN. Keep local YOLO/rules and Ollama `qwen3-vl:2b-instruct`, no cloud key or runtime Codex/GitHub dependency. Use the existing project-local loopback port 11435 and disable Ollama cloud; verify the target config instead of inheriting init port 11434. Reuse the verified Windows environment, package missing pinned Windows dependencies and complete model assets for local delivery, and keep Seetong network reachability separate from local AI independence. Domestic cloud is an unenabled future option requiring explicit image/fee authorization and a distinct adapter. The required real Windows introduction is now 15–30 minutes (about 20 recommended), including a continuous 300-second absence scene; old synthetic footage does not satisfy it. This decision does not establish package completion or target-network/runtime acceptance.
- D014 (2026-09-25, Seetong capture evidence): The integrity-verified 2026-09-23 package remains a draft-review candidate. Exact-HWND product and provider-only paths each produced one callback, while one separately authorized display WGC observation produced 248 callbacks/unique capture timestamps in 8.109 seconds. This isolates a promising display path, not video ROI freshness, FPS, root cause, or field acceptance. Do not repeat the 8-second test before reading its raw logs/version/source metadata; do not infer Windows Git state from public main, auto-upgrade dependencies/drivers, buy hardware, or change models.
- D015 (2026-09-25, controlled display route): The operational path is authorized display WGC plus Seetong video-region cropping and observability protection. Continuing capture needs explicit scope authorization because post-crop cannot remove full-screen exposure. The Windows implementation must bind display identity, client geometry/DPI/position, mapping version and ROI; invalidate across backends; and pause/reset absence on obstruction, minimize, lock, source change or unobservable ROI. Notifications must not cover/re-capture monitoring ROI. A 5-minute single-CH9 video-clock/visible-action/ROI-change check is required before real 1→3→10 YOLO/Qwen/alert/video work. Native WGC, version isolation and DXGI are fallback diagnostics only if the display-video ROI route fails; RTSP remains conditional on existing authorized interfaces.
